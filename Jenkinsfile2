pipeline {
    agent any

    stages {
        stage('Install-Nginx') {
            steps {
             sh '''
               kubectl run nginx --image=nginx --context docker-desktop
               kubectl get pods --context docker-desktop
             '''
            }
        }
    }
}
