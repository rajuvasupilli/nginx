pipeline {
    agent any

    stages {
        stage('Install-Nginx') {
            steps {
             sh '''
               kubectl create nginx --image=nginx --context docker-desktop
             '''
            }
        }
    }
}
